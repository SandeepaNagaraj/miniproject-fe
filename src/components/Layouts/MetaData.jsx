import { Helmet } from "react-helmet-async";

const MetaData = ({ title }) => {
    return (
        <Helmet>
            <title>Circle of FOOD</title>
        </Helmet>
    );
};

export default MetaData;